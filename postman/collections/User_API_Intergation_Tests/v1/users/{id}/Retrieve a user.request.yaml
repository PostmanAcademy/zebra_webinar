$kind: http-request
name: Retrieve a user
description: ""
url: "{{baseUrl}}/v1/users/:id"
method: GET
headers:
  Accept: application/json
pathVariables:
  - key: id
    value: "{{id}}"
    description: ""
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "integer"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: text
  content: ""
scripts:
  - type: afterResponse
    code: |-
      pm.test("User retrived successfully", function () {
          pm.response.to.have.status(200);
      });
    language: text/javascript
settings: {}
examples: ./.resources/Retrieve a user.resources/examples
order: 1000
